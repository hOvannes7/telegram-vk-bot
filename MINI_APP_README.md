# 📱 Telegram Mini App для VK Bot

## Что такое Mini App?

Telegram Mini App — это веб-приложение, которое открывается прямо внутри Telegram. Пользователи могут удобно настраивать параметры копирования постов через красивый интерфейс.

## 🚀 Развёртывание Mini App

### Вариант 1: GitHub Pages (бесплатно, рекомендуется)

#### 1. Создайте репозиторий на GitHub

```bash
cd D:\QWEN\telegram-vk-bot
git init
git add .
git commit -m "Initial commit"
```

#### 2. Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Создайте новый репозиторий (например, `telegram-vk-bot`)
3. Следуйте инструкциям для push существующего кода

```bash
git remote add origin https://github.com/ВАШ_USERNAME/telegram-vk-bot.git
git branch -M main
git push -u origin main
```

#### 3. Включите GitHub Pages

1. Зайдите в настройки репозитория
2. Перейдите в **Pages**
3. В разделе **Source** выберите:
   - Branch: `main`
   - Folder: `/(root)`
4. Нажмите **Save**

Через 1-2 минуты ваш сайт будет доступен по адресу:
```
https://ВАШ_USERNAME.github.io/telegram-vk-bot/webapp/
```

#### 4. Обновите URL в боте

Откройте `bot.py` и измените `MINI_APP_URL`:

```python
MINI_APP_URL = "https://ВАШ_USERNAME.github.io/telegram-vk-bot/webapp/"
```

#### 5. Перезапустите бота

```bash
python bot.py
```

---

### Вариант 2: Vercel (бесплатно)

#### 1. Установите Vercel CLI

```bash
npm install -g vercel
```

#### 2. Разверните проект

```bash
cd webapp
vercel --prod
```

#### 3. Обновите URL в боте

Используйте URL, который предоставит Vercel.

---

### Вариант 3: Любой хостинг

Просто загрузите файлы из папки `webapp/` на любой хостинг с поддержкой статических сайтов.

---

## 🔧 Настройка в Telegram

### 1. Откройте BotFather

1. Найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите вашего бота

### 2. Настройте Menu Button

1. Отправьте `/setmenubutton`
2. Выберите бота
3. Отправьте URL вашего Mini App:
   ```
   https://ВАШ_USERNAME.github.io/telegram-vk-bot/webapp/
   ```
4. Введите название кнопки: `Открыть панель`

### 3. (Опционально) Настройте Web App для бота

1. Отправьте `/setchatmenubutton`
2. Выберите бота
3. Отправьте URL Mini App

---

## 📱 Как использовать

1. Откройте бота в Telegram
2. Нажмите кнопку **"📋 Открыть панель управления"** рядом с полем ввода
3. Или отправьте команду `/webapp`

В Mini App вы можете:
- 👉 Ввести название или ID группы VK
- 📅 Выбрать даты начала и окончания периода
- 📊 Указать количество постов (ползунком или числом)
- 📤 Выбрать куда отправлять посты

После нажатия кнопки **"🚀 Начать копирование"** бот получит данные и начнёт копирование!

---

## 🎨 Особенности Mini App

- ✅ **Адаптивный дизайн** — работает на всех устройствах
- 🌙 **Тёмная тема** — автоматически подстраивается под тему Telegram
- 📱 **Нативная интеграция** — использует Telegram WebApp API
- ⚡ **Мгновенная загрузка** — лёгкий и быстрый интерфейс
- 🔔 **Haptic feedback** — тактильная отдача при взаимодействии

---

## 🛠️ Разработка и изменение

### Структура файлов Mini App

```
webapp/
├── index.html    # HTML разметка
├── style.css     # Стили и оформление
└── app.js        # Логика и интеграция с Telegram
```

### Изменение цветов

В `style.css` используйте CSS переменные Telegram:

```css
var(--tg-theme-bg-color)         /* Фон */
var(--tg-theme-text-color)       /* Текст */
var(--tg-theme-button-color)     /* Цвет кнопок */
var(--tg-theme-hint-color)       /* Вторичный текст */
```

### Добавление новых полей

1. Добавьте HTML элемент в `index.html`
2. Получите значение в `app.js`
3. Отправьте через `tg.sendData()`
4. Обработайте в `bot.py` в `handle_webapp_data()`

---

## 📊 Мониторинг

Логи Mini App доступны в консоли браузера (F12) и в файле `bot.log`.

---

## ⚠️ Важные замечания

1. **URL должен быть HTTPS** — Telegram требует безопасное соединение
2. **CORS** — убедитесь, что хостинг разрешает загрузку с telegram.org
3. **Размер** — старайтесь держать размер файлов небольшим для быстрой загрузки
4. **Кэширование** — GitHub Pages может кэшировать файлы до 1 минуты

---

## 🎉 Готово!

Теперь ваш бот имеет современный веб-интерфейс для удобного управления копированием постов!
