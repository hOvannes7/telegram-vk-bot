# 🤖 Telegram VK News Bot

Современный Telegram-бот для копирования постов из сообществ ВКонтакте (VK) в Telegram с сохранением всех медиа-вложений (фото, видео, документы, ссылки).

## ✨ Возможности

- **Копирование постов из VK** - Загрузка постов из любого публичного сообщества VK
- **Фильтрация по датам** - Копирование постов за определённый период
- **Полная поддержка медиа** - Автоматическая загрузка и пересылка:
  - 📷 Фотографии (во всех разрешениях)
  - 🎬 Видео (с обложками)
  - 📄 Документы/файлы
  - 🔗 Ссылки с предпросмотром
- **Группировка фото** - Несколько фото отправляются как альбом
- **Отслеживание прогресса** - Обновления в реальном времени во время копирования
- **Обработка ошибок** - Надёжная система обработки ошибок с логированием
- **Современная архитектура** - Построен на python-telegram-bot v21+ (async)

## 📋 Требования

- Python 3.9+
- Токен Telegram-бота
- Токен доступа VK API

## 🚀 Установка

### 1. Перейдите в директорию проекта

```bash
cd telegram-vk-bot
```

### 2. Создайте виртуальное окружение (рекомендуется)

```bash
python -m venv venv
```

Активируйте виртуальное окружение:

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Получите токен Telegram-бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте токен бота

### 5. Получите токен доступа VK API

1. Перейдите на [VK Apps](https://vk.com/editapp?act=create)
2. Создайте новое приложение (тип "Standalone-приложение")
3. В настройках приложения получите access token с правами:
   - `wall` - Доступ к записям на стене
   - `photos` - Доступ к фотографиям
   - `video` - Доступ к видео
   - `docs` - Доступ к документам

Альтернативно можно использовать [VK Token Helper](https://vkhost.github.io/) для быстрой генерации токена.

### 6. Настройте переменные окружения

1. Скопируйте пример файла окружения:

**Windows:**
```bash
copy .env.example .env
```

**Linux/Mac:**
```bash
cp .env.example .env
```

2. Отредактируйте `.env` и добавьте ваши токены:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_telegram_бота
VK_ACCESS_TOKEN=ваш_токен_vk_api
VK_API_VERSION=5.131
TARGET_CHAT_ID=
```

## 📖 Использование

### Запуск бота

```bash
python bot.py
```

### Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Приветственное сообщение |
| `/copy` | Начать копирование постов из VK |
| `/help` | Показать справку |
| `/status` | Проверить статус бота |
| `/cancel` | Отменить текущую операцию |

### Процесс копирования

1. Отправьте боту команду `/copy`
2. Введите название или ID группы VK (например, `durov` или `123456`)
3. Введите начальную дату в формате `ГГГГ-ММ-ДД` (например, `2024-01-01`)
4. Введите конечную дату в формате `ГГГГ-ММ-ДД` (например, `2024-12-31`)
5. Введите количество постов для копирования (1-100, по умолчанию 50)
6. Дождитесь завершения копирования

### Пример

```
/copy
→ Группа: durov
→ Начальная дата: 2024-01-01
→ Конечная дата: 2024-01-31
→ Количество: 50
```

Бот скопирует до 50 постов из сообщества "durov", опубликованных в период с 1 по 31 января 2024 года.

## 📁 Структура проекта

```
telegram-vk-bot/
├── bot.py              # Основное приложение бота
├── config.py           # Управление конфигурацией
├── vk_client.py        # Клиент VK API
├── media_handler.py    # Обработчик загрузки/выгрузки медиа
├── requirements.txt    # Python зависимости
├── .env.example        # Пример файла окружения
├── .env                # Ваш файл окружения (создаётся из .env.example)
├── bot.log             # Логи бота (создаётся автоматически)
└── README.md           # Этот файл
```

## 🔧 Расширенная конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен вашего Telegram-бота | Обязательно |
| `VK_ACCESS_TOKEN` | Токен доступа VK API | Обязательно |
| `VK_API_VERSION` | Версия VK API | `5.131` |
| `TARGET_CHAT_ID` | Чат по умолчанию (опционально) | Пусто |

### Логи

Логи бота сохраняются в файл `bot.log`. Проверяйте этот файл для отладки.

## ⚠️ Ограничения

- Максимум 100 постов за одну операцию копирования
- Лимит размера файла Telegram: 50MB для ботов
- Применяются ограничения VK API (примерно 3 запроса в секунду)
- Некоторые видео VK могут быть недоступны для скачивания из-за настроек приватности

## 🛠️ Решение проблем

### Бот не отвечает

1. Проверьте, запущен ли бот
2. Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
3. Проверьте `bot.log` на наличие ошибок

### Ошибка "Group not found"

1. Убедитесь, что группа публичная
2. Попробуйте использовать ID группы вместо названия
3. Проверьте, что `VK_ACCESS_TOKEN` действителен

### Посты не копируются

1. Проверьте правильность диапазона дат
2. Убедитесь, что в группе есть посты за этот период
3. Проверьте права бота в целевом чате

## 📝 Лицензия

Проект предоставляется "как есть" в образовательных целях.

## 🤝 Вклад

Не стесняйтесь создавать issues и предлагать улучшения!

## 📞 Поддержка

По вопросам и за поддержкой обращайтесь, создав issue на GitHub.
